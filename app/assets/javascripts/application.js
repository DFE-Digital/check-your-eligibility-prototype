//
// For guidance on how to add JavaScript see:
// https://prototype-kit.service.gov.uk/docs/adding-css-javascript-and-images
//

// const express = require('express');
// const path = require('path');
// const govukPrototypeKit = require('govuk-prototype-kit');
// const mainRouter = require('../routes'); // Adjust the path as necessary

// const app = express();

// Make the data folder available at the /data URL
// app.use('/data', express.static(path.join(__dirname, '../data'))); // Adjust the path as necessary

// Use the main router for handling routes
// app.use(mainRouter);

// Add other middleware configurations here if needed
// app.use(govukPrototypeKit.middleware); // Add GOV.UK Prototype Kit middleware if required

// Set the view engine (if using views)
// app.set('view engine', 'ejs'); // Adjust if you're using a different templating engine

// Start the server
// const PORT = 3002;
// app.listen(PORT, () => {
//   console.log(`Server is running on port ${PORT}`);
// });


// window.GOVUKPrototypeKit.documentReady(() => {
//   // Add JavaScript here
// })
// // app.js
// //const express = require('express');
// //const app = express();

// // Make the data folder available at the /data URL
// //router.use('/data', express.static(path.join(__dirname, '../data'))); // Adjust the path as necessary

// //app.use('/data', express.static(path.join(__dirname + '/data')));

// const mainRouter = require('../routes');

// app.use(mainRouter);

// // Add other configurations and middleware

// const PORT = 3002;
// app.listen(PORT, () => {
//   console.log(`Server is running on port ${PORT}`);
// });
